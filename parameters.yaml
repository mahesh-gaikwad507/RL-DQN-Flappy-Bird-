flappybirdv0:
  env_id: FlappyBird-v0
  epsilon_init: 1.0
  epsilon_min: 0.05
  epsilon_decay: 0.9995
  replay_memory_size: 100000
  mini_batch_size: 32
  # Increased because we now sync based on STEPS, not EPISODES
  network_sync_rate: 1000 
  # Lowered slightly for smoother convergence
  alpha: 0.0005 
  gamma: 0.99
  reward_threshold: 1000